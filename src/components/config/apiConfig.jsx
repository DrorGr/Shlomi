// This file can be used to store API-related configurations.
// For example, base URLs, API keys, or other settings.

const apiConfig = {
  // Example: API_BASE_URL: 'https://api.example.com/v1',
  // Example: TIMEOUT: 5000,
};

export default apiConfig;